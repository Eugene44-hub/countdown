const proposed = new Date('2021-12-25');
const current = new Date();

const arr = [proposed.getHours(), proposed.getMinutes(), proposed.getSeconds(), proposed.getFullYear()]
console.log(arr[3])
console.log(current)