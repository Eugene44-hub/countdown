const proposed = new Date('2021-12-25');
const current = new Date();

const arr = [proposed.getHours(), proposed.getMinutes(), proposed.getSeconds(), proposed.getDate()]
console.log(arr[2])
console.log(current)